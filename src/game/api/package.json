{
  "name": "game-api",
  "version": "1.0.0",
  "description": "Game api module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=./.env.local dist/index.js",
    "bs": "npm run build && npm run start",
    "format": "prettier --write .",
    "lint:format": "prettier --check ."
  },
  "keywords": [],
  "author": "amethttp",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "fastify": "4.29",
    "fastify-plugin": "^5.1.0",
    "fastify-socket.io": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "@types/socket.io": "^3.0.1",
    "prettier": "^3.3.3",
    "typescript": "^5.9.2"
  }
}
